from setuptools import setup

setup(
        name="sdtt"
        version="0.1.0",
        description="A simple detection theory tool",
        author="Stuart B. Moore",
        packages=["sdtt"],
        install_requires=["pandas", "numpy", "scipy"]
        )
